// Placeholder so Swift Package Manager has at least one source file in
// the `DubCore` target before `scripts/build-xcframework.sh` has run.
//
// Once the bootstrap script has executed, the real bindings land in
// `Sources/DubCore/Generated/` alongside this file. The package
// continues to compile with both files present.
